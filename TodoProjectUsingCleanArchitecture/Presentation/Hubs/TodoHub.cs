using Microsoft.AspNetCore.SignalR;

namespace TodoProjectUsingCleanArchitecture.Presentation.Hubs
{
    public class TodoHub : Hub
    {
        // Hub methods can be added here if clients need to call them
        // For now, we only need it for broadcasting from the server
    }
}
